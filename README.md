# Playwork Windows release runner

This public repository runs native Windows release proofs for public Playwork
artifacts. The Playwork source repository stays private and is read through a
short-lived, read-only deploy key. No source archive is published.

The workflow is manual-only and pinned to the exact release commit.
